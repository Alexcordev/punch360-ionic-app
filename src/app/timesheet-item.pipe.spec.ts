import { TimesheetItemPipe } from './timesheet-item.pipe';

describe('TimesheetItemPipe', () => {
  it('create an instance', () => {
    const pipe = new TimesheetItemPipe();
    expect(pipe).toBeTruthy();
  });
});
